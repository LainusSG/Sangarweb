services:
  web:
    build:
      context: ./ 
      dockerfile: Dockerfile
    command: >
      gunicorn --env DJANGO_SETTINGS_MODULE=backendbarber.settings
      backendbarber.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - static_volume:/root/backendbarber/staticfiles
      - media_volume:/root/backendbarber/mediafiles
    expose:
      - "8000"
    env_file:
      - ./.env

  nginx:
    build: ./nginx
    volumes:
      - static_volume:/root/backendbarber/staticfiles
      - media_volume:/root/backendbarber/mediafiles
    ports:
      - "20000:80"   # Cambié a 10000
    depends_on:
      - web

volumes:
  static_volume:
  media_volume: